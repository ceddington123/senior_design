#include "wpmotor.h"

/*void turnTo(int left, int fromDSP){
	//LEFT -> INCREASE FREQ		RIGHT -> DECREASE FREQ
	if( (left == 1 && fromDSP > 0) || (left == 0 && fromDSP < 0 ) ){
		for(int i = 0; i < fromDSP; i++)
			motorLeft(fromDSP);
	}
	//LEFT -> DECREASE FREQ		RIGHT -> INCREASE FREQcccccccccccccccccccccccccccccccccccccccccc
	else if( (left == 1 && fromDSP > 0) || (left == 0 && fromDSP < 0 ) ){
		for(int i = 0; i < fromDSP; i++)
			motorRight(fromDSP);
	}
	motorStop(100);

};*/